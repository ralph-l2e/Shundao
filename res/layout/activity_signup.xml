<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/signup_bg"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".SignupActivity" >

    <TableLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content" >

        <EditText
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/edittext_bg"
            android:drawableLeft="@drawable/sign_mobile_icon"
            android:hint="@string/mobile_hint"
            android:padding="@dimen/edittext_padding" />

        <EditText
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/edittext_bg"
            android:drawableLeft="@drawable/sign_nickname_icon"
            android:hint="@string/nickname_hint"
            android:padding="@dimen/edittext_padding" />

        <EditText
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/edittext_bg"
            android:drawableLeft="@drawable/sign_password_icon"
            android:hint="@string/psd_hint"
            android:inputType="textPassword"
            android:padding="@dimen/edittext_padding" />

        <EditText
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/edittext_bg"
            android:drawableLeft="@drawable/sign_password_icon"
            android:hint="@string/psd_confirm_hint"
            android:inputType="textPassword"
            android:padding="@dimen/edittext_padding" />

        <EditText
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/edittext_bg"
            android:drawableLeft="@drawable/sign_sex_icon"
            android:hint="@string/sex_hint"
            android:padding="@dimen/edittext_padding" />

        <EditText
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/edittext_bg"
            android:drawableLeft="@drawable/sign_type_icon"
            android:hint="@string/type_hint"
            android:padding="@dimen/edittext_padding" />

        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/sign_button"
            android:text="@string/signup_button" />
    </TableLayout>

</LinearLayout>